@page
@model SpaBookingApp.Pages.Categories.CreateCategoryModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{ Layout = "_LayoutWithUsername"; }
@{
    ViewData["Title"] = "Create Category";
}
<h1>Create Category</h1>

<form method="post" id="createCategoryForm" class="form-container">
    <div class="form-group">
        <label for="Category_Name" title="Category Name">Category Name:</label>
        <input type="text" id="Category_Name" name="Category_Name" class="form-control" placeholder="Enter category name" />
        <span class="text-danger" id="Category_Name_Error"></span>
    </div>
    <div class="form-group">
        <button type="submit" class="button primary-button">Create</button>
    </div>
</form>

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
    <p>@Model.SuccessMessage</p>
    <a href="/Categories/Index" class="button secondary-button">Back to Categories</a>
}

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <p>@Model.ErrorMessage</p>
}

@if (!ModelState.IsValid)
{
    <p>Please fix the errors in the form.</p>
}
<script src="~/js/Category/createCategory.js"></script>
